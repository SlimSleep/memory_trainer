"""
Базовый класс для всех экранов приложения.
Все экраны должны наследоваться от этого класса для обеспечения единообразного интерфейса.
"""


class Screen:
    """
    Абстрактный базовый класс для всех экранов приложения.
    
    Определяет список методов, которые должны быть реализованы в подклассах.
    Предоставляет общие свойства для управления локализацией, шрифтами и менеджером.
    """
    
    def __init__(self, manager, localizer, font):
        """
        Инициализация экрана.
        
        :param manager: объект ScreenManager для управления переключением экранов
        :param localizer: объект Localizer для работы с локализацией
        :param font: шрифт Pygame для отображения текста
        """
        self.manager = manager
        self.loc = localizer
        self.font = font


    def on_enter(self):
        """
        Вызывается когда экран становится активным.
        
        Используется для инициализации состояния, загрузки ресурсов,
        обновления локализованного текста и других подготовительных операций.
        """
        pass

    def on_exit(self):
        """
        Вызывается когда экран становится неактивным.
        
        Используется для сохранения состояния, освобождения ресурсов,
        остановки активных процессов.
        """
        pass

    def handle_event(self, event):
        """
        Обрабатывает события (клики мыши, нажатия клавиш, и т.д.).
        
        :param event: объект pygame.event.Event
        """
        pass

    def update(self):
        """
        Обновляет логику экрана каждый кадр.
        
        Используется для анимаций, таймеров, проверок состояния и т.д.
        Вызывается один раз за кадр перед draw().
        """
        pass

    def draw(self, screen):
        """
        Отрисовывает содержимое экрана на поверхность.
        
        :param screen: поверхность Pygame (pygame.Surface) для отрисовки
        """
        pass
